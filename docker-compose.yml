version: '3'

services:
  mysql:
    image: mysql:8.0.30-debian
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: allanvargensbrasil
      MSQL_DATABASE: Portfolio
    ports:
      - 3306:3306
    volumes:
      - .docker/mysql:/var/lib/mysql